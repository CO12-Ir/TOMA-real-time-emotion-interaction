# 🍅 TOMA —— 拟人化语音情绪交互虚拟宠物

TOMA 是一个基于语音情绪的虚拟宠物系统。  
它不关注你“说了什么”，而是关注你“是用什么语气在说”。

通过实时分析说话时的情绪变化，TOMA 会像宠物一样，
用不同的表情对你的情绪做出反应。

这个项目是一个侧重于交互体验的实验性原型，
许多行为经过了简化。

---

## 项目在做什么？

TOMA 会在后台持续监听麦克风输入：

- 一旦检测到你开始说话，就自动开始录音；
- 当你停止说话或录音时间过长时，自动截断语音；
- 对这段语音进行情绪分析；
- 根据分析结果，更新虚拟宠物的情绪状态和表情。

整个过程实时、自动且不需要按键触发。

---

## 核心流程说明

1. **语音监听**
   - 基于音量阈值判断是否存在语音活动；
   - 记录静音时长，保证截断位置质量
   - 录音时间过长自动截断。

2. **语音情绪识别**
   - 使用预训练的语音情绪识别模型；
   - 输出情绪标签和 VAD（三维情绪向量）。
   - 进行平滑化输出，确保情绪不过分跳跃

3. **情绪到行为映射**
   - 手动标注预期宠物反应在情绪标签和 VAD（三维情绪向量）上，训练模型
   - 使用该自制的 response 分类器，将情绪映射为宠物反应。

4. **视觉反馈**
   - 宠物的情绪状态由 Pygame 实时渲染；
   - 不同情绪对应不同表情图片。

---

## 设计思路

这个项目使得虚拟宠物模拟现实中的宠物，比起说话内容，更加反馈说话语气。
通过情绪识别和总结自现实宠物的反应，以求达到情绪陪伴的效果

---

## 适合场景与展望

- 虚拟宠物 / 桌面小组件
- 人机情绪交互实验
- 未来可接入游戏或 Live2D 表情驱动，精细化人机交互流程

## 使用方式

### 1. 环境准备

请确保已安装 Python 3.9 或以上版本。

在项目根目录下，安装所需依赖：

```bash
pip install -r requirements.txt
```
### 2. 启动程序

在项目根目录下运行：
```bash
python main.py
```
程序启动后将自动打开一个窗口，显示虚拟宠物的初始表情。

### 3. 使用说明

- 程序启动后会在后台持续监听麦克风输入；
- 当你开始说话时，系统会自动开始录音；
- 停止说话后，语音会被自动截断并进行情绪分析；
- 虚拟宠物会根据你的语气变化，实时切换表情。
- 摸摸（点击）宠物的头，可以让其变得开心


> English documentation is available in `README.md`.
